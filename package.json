{
  "name": "on-this-day-agent-newsroom",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vercel dev",
    "check": "node --check api/agents/run-all.js && node --check lib/pipeline.js"
  }
}