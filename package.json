{
  "name": "on-this-day-agent-newsroom",
  "version": "3.4.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vercel dev",
    "check": "node --check api/router.js && node --check lib/agents.js && node --check lib/queued-pipeline.js && node --check lib/openai.js && node --check lib/routes/agents-run-all.js && node --check lib/routes/agents-status.js && node --check lib/routes/health.js"
  },
  "dependencies": {
    "@vercel/functions": "3.9.5"
  }
}
